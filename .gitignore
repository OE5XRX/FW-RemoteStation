# editors
*.swp
*~

# python
.venv

# build
/build*
/twister-out*
/app/build*
.cache/

__pycache__/
eeprom.bin

# native_sim runtime artifact: persisted synthetic device UID (device_uid.cpp)
oe5xrx_sim_uid.txt

# codeql
_codeql_detected_source_root

# docs
/doc/_build*

# implement later
doc
*.wav

# FW signing keys — NEVER commit the private key (Teilbereich E)
keys/
*-ecdsa-p256.pem
oe5xrx-fw*.pem
!release/signing/oe5xrx-fw-public.pem
