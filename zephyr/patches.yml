# Downstream patches applied to west modules via 'west patch apply'.
# (empty) The STM32 UDC isochronous-incomplete recovery is now upstream in
# Zephyr (PR #113622), present at the pinned SHA, so no downstream patch is
# needed. Re-add here only if a future pin predates an upstream fix.
patches: []
